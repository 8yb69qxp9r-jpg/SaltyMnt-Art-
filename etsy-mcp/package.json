{
  "name": "etsy-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for Etsy shop management — monitor health, manage listings, track orders",
  "scripts": {
    "start": "node server.js",
    "auth": "node auth.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
